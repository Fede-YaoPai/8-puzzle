{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:typescript && npm run build:sass && npm run build:webpack",
    "build:typescript": "tsc",
    "build:sass": "sass src/styles/styles.scss dist/styles/styles.css --no-source-map",
    "build:webpack": "npx webpack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.69.5",
    "typescript": "^5.2.2",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  },
  "dependencies": {
    "primeflex": "^3.3.1"
  }
}
